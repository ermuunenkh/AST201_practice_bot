import json
from config import RAW_POOL_PATH, POOL_PATH


def build_question_pool() -> None:
    # TODO: validate fields, generate images for questions with "image_gen" instructions,
    #       update "image" paths in pool, and write cleaned pool back to POOL_PATH
    pass
